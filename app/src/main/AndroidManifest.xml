<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.aku.hassannaqvi.willows_hhlisting">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the username's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".Core.AppMain"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".Activities.HouseholdListingActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.AddWomenActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.setupActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.FamilyListingActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Core.AndroidDatabaseManager"
            android:screenOrientation="portrait"
            android:theme="@style/android:Theme.Holo.Light.DarkActionBar" />
        <activity
            android:name=".Activities.LoginActivity"
            android:label="Login In"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.SplashScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>